# expect: auth.credentials_file
listener: { address: ":8008", plaintext: true }
auth: { mode: resign, credentials_file: creds.yaml }
control: { endpoints: ["http://10.0.0.1:9901"], plaintext: true, cache_dir: /var/lib/shunt/cache }
