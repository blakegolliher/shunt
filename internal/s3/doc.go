// Package s3 is the request model: bucket and key extraction for virtual-host and path style,
// the operation classifier (a table, not an if-ladder), and S3 error XML rendering
// (docs/DESIGN.md §1.2). Built in POC-1.
package s3
