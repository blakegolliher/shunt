# expect: placements.acme/data.ramp.hash
version: 1
tenants: { acme: { default_cluster: garage } }
placements:
  acme/data: { state: RAMPING, primary: minio, source: garage, ramp: { ratio: 0.5 }, names: { garage: data, minio: data } }
