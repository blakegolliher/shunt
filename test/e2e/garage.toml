# Single-node Garage for e2e (validated on v2.3.0; no 1.x → 2.x key changes needed). Keys below are test-only.
metadata_dir = "/var/lib/garage/meta"
data_dir = "/var/lib/garage/data"
db_engine = "sqlite"
replication_factor = 1

rpc_bind_addr = "[::]:3901"
rpc_public_addr = "127.0.0.1:3901"
rpc_secret = "1799bccfd7411eddcf9ebd316bc1f5287ad12a68094e1c6ac6abde7e6feae1ec"

[s3_api]
s3_region = "garage"
api_bind_addr = "[::]:3900"
root_domain = ".shunt.example.com"   # passthrough: shunt preserves Host, so the backend must own the shunt domain

[admin]
api_bind_addr = "[::]:3903"
admin_token = "shunt-e2e-admin-token"
metrics_token = "shunt-e2e-metrics-token"
